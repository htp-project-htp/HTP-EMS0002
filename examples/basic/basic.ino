/*
  ******************************************************
  ⚠️ تحذير هام – جهاز تحفيز العضلات ⚠️
  ******************************************************
  
  هذا الكود يتحكم في وحدة تحفيز عضلي.
  
  ❗ الاستخدام المطوّل للجهاز على عضلات الجسم قد يسبب إجهادًا عضليًا أو تهيجًا جلديًا.
  ❗ لا يُوصى باستخدام ترددات أعلى من 100 هرتز أو زيادة الطاقة الكهربائية دون استشارة مختص.
  ❗ يُمنع الاستخدام على مناطق حساسة من الجسم (مثل الرأس أو القلب).
  ❗ لا تستخدم الجهاز إن كنت تعاني من أمراض في القلب أو لديك جهاز منظم ضربات.
  ❗ تأكد دائمًا من إيقاف التشغيل قبل توصيل أو فصل الجهاز من العضلات.

  📌 هذا الكود والجهاز لأغراض تعليمية وتجريبية فقط، وليس مخصصًا للاستخدام الطبي أو العلاجي دون إشراف طبي.


  ******************************************************
*/


/*
  📋 حدود التشغيل الآمن لوحدة تحفيز العضلات:

  ▸ التردد (Frequency):
    - المدى: من 0 إلى 50 هرتز (Hz)
    - ⚠️ يوصى بعدم تجاوز 50 هرتز لتجنب الإجهاد العصبي أو العضلي.

  ▸ قوة النبضة (Duty Cycle):
    - النطاق: من 0% إلى 100%
    - ⚠️ يُفضل عدم تجاوز 70% لفترات طويلة لتفادي التحفيز الزائد.

  ▸ توقيت التشغيل (ON Time):
    - المدى: من 100 إلى 2000 مللي ثانية (ms)
    - ⏱️ أقصى مدة تشغيل مستمرة: 2 ثانية

  ▸ توقيت الإطفاء (OFF Time):
    - المدى: من 0 إلى 5000 مللي ثانية (ms)
    - ⏱️ أقصى مدة إيقاف: 5 ثوانٍ

  ❗ هذه القيم تم تحديدها لضمان السلامة أثناء الاستخدام.
*/


/*
 ربط الجهاز مع المتحكم 
 
 Vcc - 5V
 GND - GND 
 PWM - PIN 3
 
 */

#include <HTP_EMS0002.h> // استدعاء مكتبة MuscleStim التي تُستخدم للتحكم بجهاز تحفيز العضلات

#define STIM_PIN 3 // تعريف رقم المنفذ الرقمي 3 لتوصيل جهاز التحفيز العضلي

MuscleStim stim(STIM_PIN); // إنشاء كائن باسم stim من الفئة MuscleStim وتحديد المنفذ الذي سيتم استخدامه

void setup() {
    stim.begin(); // تهيئة جهاز التحفيز للبدء بالعمل (تشغيل الجهاز وتحضير الإعدادات)
    stim.setFrequency(50);               //  تردد النبضات الكهربائية على 10 هرتز ( يمكنك تغيير القيم حسب الحاجة)
    stim.setDutyCycle(100);              // الشدة 15% – احرص على اختيار ما يناسبك فاستجابة الجسم تختلف من شخص لآخر
    stim.setOnOffTimers(1500, 100);        //  وقت التشغيل على 1500 ملي ثانية ووقت الإيقاف على 100 ملي ثانية (يمكنك تغيير القيم حسب الحاجة)
    stim.start(); // بدء تشغيل جهاز التحفيز العضلي بعد ضبط الإعدادات
}

void loop() {
    stim.update();  // يجب استدعاء هذه الدالة بشكل متكرر في كل دورة لتحديث حالة الجهاز وضمان عمله الصحيح
}
